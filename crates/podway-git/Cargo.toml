[package]
name = "podway-git"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
podway-core = { path = "../podway-core" }
sha2.workspace = true
uuid.workspace = true
rustix = { version = "1.1.4", features = ["fs"] }

[dev-dependencies]
libc = "0.2"
tempfile.workspace = true
