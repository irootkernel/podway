{
  "fixture_version": "podway.phase0.schema-0-uninitialized/v1",
  "fixture_kind": "empty-predecessor-contract",
  "scope": {
    "proves": [
      "schema-0/uninitialized identity",
      "empty predecessor has no user-schema objects or rows",
    ],
    "does_not_prove": [
      "an on-disk SQLite database",
      "production Store behavior",
      "production Git behavior",
      "production daemon behavior",
      "production Service behavior"
    ]
  },
  "predecessor": {
    "identity": "schema-0-uninitialized",
    "schema": "schema-0",
    "storage": "on-disk-sqlite-empty",
    "is_database_file": true,
    "tables": [],
    "rows": []
  },
  "expected_schema_v1": {
    "identity": "schema-v1",
    "user_version": 1,
    "ddl_path": "spec/sqlite-v1.sql",
    "required_pragmas": {
      "foreign_keys": "ON",
      "journal_mode": "WAL",
      "synchronous": "FULL",
      "busy_timeout": 5000,
      "trusted_schema": "OFF"
    }
  },
  "expected_assertions": [
    {
      "id": "initial-migration-atomicity",
      "SchemaBeforeCommit rollback leaves user_version=0 and no user-schema objects; initialization uses an IMMEDIATE SQLite transaction, not file publication"
    },
    {
      "id": "initial-migration-no-loss",
      "the empty predecessor has no task or other application rows; schema-v1 creates its own required singleton workspace_state row"
    },
    {
      "id": "initial-migration-no-duplicate",
      "reopening schema-v1 does not re-run initialization; schema_migrations remains a single version-1 row"
    }
  ],
  "requirement_ids": ["STO-011", "REL-006"]
}
