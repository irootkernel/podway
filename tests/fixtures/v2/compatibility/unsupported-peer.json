{
  "schema": "podway.v2-fixture-recipe/v1",
  "fixture_class": "compatibility",
  "evidence_level": "contract-recipe",
  "implementation_status": "planned",
  "cases": [
    {"id": "v2-command-to-v1-peer", "operation": "send each v2-only command to a peer without its registered capability", "expected_code": "UNSUPPORTED_V2_CAPABILITY"},
    {"id": "registered-but-unserved", "operation": "send a registered v2 command before its daemon implementation exists", "expected_code": "UNSUPPORTED_V2_CAPABILITY"},
    {"id": "absent-route", "operation": "send a command absent from the route registry", "expected": "usage-or-unknown-command-before-v2-dispatch"}
  ]
}
